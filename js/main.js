/* ============================================================
   main.js — Punto de entrada: inicializa todo al cargar la página
   ============================================================ */

window.addEventListener('DOMContentLoaded', () => {
  initStars();
  initParticles();
  initCursorSparkles();
  buildHomeCatalog();
});
